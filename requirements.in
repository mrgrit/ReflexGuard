# Phase 1 runtime and validation tools, resolved for Python 3.10.
fastapi
httpx
pydantic>=2,<3
uvicorn
pytest
pip
setuptools

# Phase 3 camera encoding (headless wheels for SSH/Webots).
numpy
opencv-python-headless

# Phase 4 authenticated control dashboard.
SQLAlchemy>=2,<3
Jinja2>=3,<4
bcrypt>=5,<6
